{
  "name": "rescript-react",
  "version": "0.0.1",
  "scripts": {
    "re:build": "bsb -make-world -clean-world",
    "re:watch": "bsb -make-world -clean-world -w",
    
    "prebuild": "yarn re:build",
    "build": "parcel build index.html",
    "prestart": "yarn re:build",
    "start": "parcel index.html"
  },
  "keywords": [
    "rescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "*",
    "parcel-bundler": "^1.12.4",
    "tailwindcss": "^1.9.6"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "reason-react": "^0.9.1"
  }
}
